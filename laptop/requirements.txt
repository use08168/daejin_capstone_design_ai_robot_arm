# 노트북(Control Hub) 의존성
django>=5.2
opencv-python>=4.12
numpy
ultralytics>=8.4
matplotlib

# torch / torchvision 은 GPU(CUDA) 빌드를 별도 설치:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# (ultralytics 가 CPU torch 를 끌어오므로, GPU 가속하려면 위 명령으로 교체)
